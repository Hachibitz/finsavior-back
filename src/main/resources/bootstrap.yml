spring:
  application:
    name: finsavior-back
  cloud:
    config:
      uri: '${finsavior-configserver-url}'
      enabled: true
      fail-fast: true
      name: '${spring.application.name}'
      label: dev

server:
  port: 8085