spring:
  application:
    name: finsavior-back
  cloud:
    config:
      uri: http://localhost:8084
      enabled: true
      fail-fast: true
      name: '${spring.application.name}'
      label: dev

server:
  port: 8085